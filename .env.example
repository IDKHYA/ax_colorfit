# 프론트 환경변수 예시. 실제 값은 .env.local(gitignore됨)에 둔다.

# 독립 ML 서비스(ml_service/) 기준 URL. 비워두면 같은 출처 상대 경로(/api/...)를 그대로 쓴다(FR-042).
# 로컬에서 `npm run dev:ml`로 ML 서비스를 띄웠다면 아래처럼 지정한다.
VITE_ML_API_BASE_URL=http://127.0.0.1:8501

# 촬영 없이 데모 결과(소프트 서머)로 시연하고 싶을 때만 true로 둔다(FR-001). 기본은 비워둔다.
VITE_DEMO_MODE=
